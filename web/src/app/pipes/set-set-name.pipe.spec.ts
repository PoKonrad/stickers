import { SetSetNamePipe } from './set-set-name.pipe';

describe('SetSetNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SetSetNamePipe();
    expect(pipe).toBeTruthy();
  });
});
